# Published full-zone source check

Verified published revision: `9b62570580449ed819e55314f7ef1a8f932be5d3`.
This record is documentation-only; it does not alter the native-tested map.

Read-only workflow: https://github.com/mai1015/zerkov/actions/runs/35172786403

- Source-contract job `105047822574` passed: twenty source/installer tests,
  twenty-three exact-output policy tests and the full repository policy command.
- Native-original-art job `105047822819` stopped at required source preflight:
  the eleven PNGs remain absent from GitHub. No original-art CI rendering is claimed.
- Source artifact `10477242653`, SHA-256
  `941434523c0b37c799dac1db46c606fcf6d915129ba50f7c4344862e3b38cea5`,
  was downloaded and verified in the local runtime.
- All 2,188 selected text records match their SHA-256 and Git blob ID records.
- All forty newly serialized `.tscn`/`.tres` resources match the exact local
  original-art native-tested resource bytes.
- Ten important implementation/test/registration files also match the local
  tested files, including the final backdrop, view controller, migration script,
  native test, runner, source tests and registered output policy.
- The temporary publication workflow is absent from the final tree. Its ephemeral
  dimensions-only serializer did not supply any images for local/native tests.

The native evidence remains 3096/0 twice, 4/0 in the automated independent edited
scene process, twelve repeatable captures per run, and 4/0 in a separate real
editor-input readback. The original-art delivery project cold-imports and launches.
Details and scope are in `FULL_ZONE_VALIDATION.md`.

This is a saved native counterpart of the complete environment. It is not active
raid-map integration, clean graphical-editor-host acceptance, a full-game
regression pass, original-art GitHub provisioning or a release approval.
PR #24 stays draft and unmerged.
