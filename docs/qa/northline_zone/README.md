# Full Northline zone: verification

Current area is 2688x1792 versus the original 672x448: exactly 16x. Nine districts,
nineteen interiors, 541 authored props plus fence runs, 1,814 decals. Sprite
normalization is unchanged. The prior two compact studies remain available.

Local validation with standard Godot 4.7.2.stable.official.ed1daf0bf:
14 full-zone Python contracts and 15 existing map-study contracts pass. The
review graph connects every district, every interior, four entry points and four
proposed exits at 6px clearance. A sealed-cross-map control is rejected.
Native tests use the actual 5px-radius CharacterBody2D, stop at a closed boundary,
walk every exit route, dispatch mouse/key input and capture twelve PNGs.
The native runner repeats the process and fails any mismatch or unexpected
engine diagnostic. Known Xvfb unsupported-V-Sync notices are counted separately.

Run from a complete checkout:

```sh
python3 -m unittest discover -s tests/tooling -p test_northline_zone.py -v
python3 -m unittest discover -s tests/tooling -p test_map_studies.py -v
python3 tests/tooling/run_northline_zone_gate.py --godot "$ZERKOV_GODOT" --output /tmp/northline-review-new
```

`capture.json` identifies the exact screenshot/source hashes; capture artifacts
contain all twelve native images. Images are not stitched, enlarged afterwards,
or generated by an image model. The overview is a genuine zoomed-out Godot view.

These are isolated environment tests, not the six-add-on app host or live raid.
The unchanged broad source-policy checker retains the original eight AI
registration/capture findings; the extension must introduce no additional issue.
User approval, full-host regression, production adapters, live AI/combat/loot,
active extraction/settlement and redistribution clearance remain unaccepted.
