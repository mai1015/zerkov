# Shoreline prototype validation

The PR scene is validated with Godot 4.7.2 (`4.7.2.stable.official.ed1daf0bf`) by a fresh editor import followed by headless startup of `res://game/presentation/shoreline_prototype/shoreline_review.tscn`.

The committed scene is an additive presentation prototype. It intentionally does not claim production NativeRaidMap compatibility.
