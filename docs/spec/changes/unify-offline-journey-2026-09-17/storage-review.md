# User-approved storage review correction

The user approved whole-section storage scrolling, secure storage last, and a vertically centered character. Two corrections are required before publishing the previously local-only storage patch:

- An unequipped rig or backpack SHALL have no visible storage grid, including when an older save contains unassigned items. Retain only the equipment slot and an honest non-grid recovery path; never delete saved items or grant replacement capacity.
- Reuse the original authored Quick Use item-slot bar. Do not introduce the proposed Fire/Reload/Melee/First Aid/Interact replacement strip. Move the existing item-slot presentation to the bottom outside scrolling, and reuse those authored controls in raid without inventing quick-slot assignments.

Keep the other approved layout changes, live equipment behavior, contextual loot, and latest merged runtime fixes. No save schema, addon lock, starter-kit, combat or map changes are authorized by this correction.

Verification must include empty and legacy-content unequipped states, equipped/unequipped transitions, full-section scrolling, secure-last order, and exactly one original-style Quick Use row. Preview samples must not be presented as confirmed assignments. Actual native captures, not image edits, are required for visual review.
