# Tasks

Status: the user approved staged implementation after reviewing the proposal ("yes, lets go"). R2 code and its differential/native measurement harness are implemented; native acceptance is pending. This approval does not declare the replacement dispatcher proven or waive R3/R5 gates. See implementation.md for delivered scope and evidence. The earlier scanner lifetime fix alone did not complete these tasks.

- [ ] R1 — Record per-stage p50/p95/p99, source/native identities, scan/body attribution and long-run growth on the actual application. Run debug and release separately. Acceptance: raw paired logs, equal authority traces, no runtime errors, no windowed-FPS claim from headless ticks. Status: p99/max and runtime/hardware identity recording added; separate release, long-run and target-hardware evidence remain outstanding.
- [ ] R2 — Optimize expected health metadata and duplicate-aware definition lookup without caching live-catalog validity. Acceptance: same rejection reasons for missing/duplicate/changed definitions, mutated nested resources and current attributes; real-native health/combat suites; measured paired delta. Status: implemented, not yet accepted. Frozen-reference mutation tests and same-run ABBA preflight/application profiling are wired into native CI.
- [ ] R3 — Review and approve the closed production dispatch contract and capability/revocation threat model before replacing per-invocation graph scans. Acceptance: explicit approved deltas and tests covering forged registration, wrong phase, stale owner/generation, reflected roster tampering, retained capability, teardown and owner loss.
- [ ] R4 — Implement the approved dispatcher, retaining the existing diagnostic/reference path until replacement acceptance is complete. Acceptance: exhaustive adversaries, ordering/idempotency equivalence, extraction/death/restart equality, per-tick dispatch cost bounded independently of unrelated retained graphs.
- [ ] R5 — Prove active-catalog immutability or complete mutation invalidation using installed APIs. Then remove redundant live validation only under the approved content contract. Acceptance: no mutable aliases; nested/resource-array mutation tests; invalidation before the next affected operation; separate addon approval/promotion if native changes are necessary.
- [ ] R6 — Reuse immutable projections with complete dependency revisions; prevent long-raid validation growth. Acceptance: every relevant mutation refreshes output, unchanged data is not reconstructed per reader, no mutable public alias, unchanged digest/save behavior, population and duration scaling evidence.
- [ ] R7 — Execute full native functional regression and windowed 1920x1080 frame-time acceptance on named hardware. Acceptance: 60 Hz simulation preserved; reported 60/120 FPS frame percentiles and missed budgets; debug/release separated; five-minute warm run; full extraction/death/save-retry/relaunch; no required diagnostics outstanding.
