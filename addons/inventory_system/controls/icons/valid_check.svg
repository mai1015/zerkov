<!-- valid_check: drop-valid / accepted-flash (DESIGN.md §7.2, §11.2).
     Literal metaphor: a single checkmark stroke. 24x24 canvas, 2px keyline
     margin, 1.5px stroke per §7.1. Drawn in white so runtime `modulate`
     (theme state color) tints it correctly -- see inventory_state_icons.gd
     header comment for why white rather than a CSS `currentColor` value. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="24" height="24">
  <path d="M4.5 12.5 L9.5 17.5 L19.5 6"
        fill="none" stroke="#FFFFFF" stroke-width="1.5"
        stroke-linecap="square" stroke-linejoin="miter"/>
</svg>
